class BloomFilter {
  constructor(size) {
    // TODO: Initialize the bit array and hash functions
  }

  add(value) {
    // TODO: Add the value to the bloom filter by setting the appropriate bits in the bit array
  }

  mayContain(value) {
    // TODO: Check if the value may be contained in the bloom filter by checking the appropriate bits in the bit array
  }

  remove(value) {
    // TODO: Remove the value from the bloom filter by unsetting the appropriate bits in the bit array
  }
}
